/// Used for autocomplete on input. Displays some set of options
/// which are provided in the props. The user can choose
/// an option by clicking on it (which this element handles)
/// or by pressing tab (which the owning element handles).